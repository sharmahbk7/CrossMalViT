# Pretrained Weights

This directory stores optional pretrained CrossMal-ViT weights.

## Download

Use `download_weights.py` to fetch weights from a URL:

```bash
python pretrained/download_weights.py --url https://example.com/crossmal_vit_best.pth
```

The script saves the file as `crossmal_vit_best.pth` in this folder.
